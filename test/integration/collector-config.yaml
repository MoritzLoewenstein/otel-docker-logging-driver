receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317

exporters:
  debug:
    verbosity: detailed
  file:
    path: /data/otel-logs.json

service:
  pipelines:
    logs:
      receivers: [otlp]
      exporters: [debug, file]
